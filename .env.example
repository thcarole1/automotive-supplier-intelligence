# Copiez ce fichier vers .env et renseignez les valeurs.
# .env est exclu de Git (voir .gitignore) : ne jamais y mettre de vrai secret ici.

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=asip
POSTGRES_USER=asip_user
POSTGRES_PASSWORD=changeme
